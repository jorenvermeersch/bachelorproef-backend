const placeRepository = require('./place');
const transactionRepository = require('./transaction');
const userRepository = require('./user');

module.exports = {
  placeRepository,
  transactionRepository,
  userRepository,
};
